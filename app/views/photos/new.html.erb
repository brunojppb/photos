
<%= form_for @photo, :url => {:action => 'create', :user_id => @user.id}, :html => { :multipart => true } do |f| %>
  <%= f.file_field :image %>
    <br>
  <%= submit_tag("Save photo", :class => "btn btn-primary") %>
<% end %>